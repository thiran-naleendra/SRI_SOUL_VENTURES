@extends('layouts.admin',['title'=>'Pages','description'=>'Manage predefined page sections and content.'])
@section('page-actions')<a class="btn btn-admin-primary" href="{{ route('admin.pages.create') }}">Add section</a>@endsection
@section('content')<div class="card admin-card mb-4"><div class="card-body"><form class="row g-3"><div class="col-md-7"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search heading or section key"></div><div class="col-md-3"><select class="form-select" name="page"><option value="">All pages</option>@foreach(['home','experiences','packages','destinations','custom_tours','about','contact'] as $page)<option value="{{ $page }}" @selected(request('page')===$page)>{{ str($page)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-admin-primary">Filter</button></div></form></div></div><div class="card admin-card"><div class="table-responsive"><table class="table admin-table mb-0"><thead><tr><th class="ps-4">Page</th><th>Section</th><th>Heading</th><th>Order</th><th>Active</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td class="ps-4">{{ str($item->page_key)->replace('_',' ')->title() }}</td><td><code>{{ $item->section_key }}</code></td><td>{{ $item->heading?:'—' }}</td><td>{{ $item->display_order }}</td><td>{{ $item->is_active?'Yes':'No' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.pages.edit',$item) }}">Edit</a><button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#confirmationModal" data-confirm-action="{{ route('admin.pages.destroy',$item) }}">Delete</button></td></tr>@empty<tr><td colspan="6"><div class="admin-empty m-3">No page sections found.</div></td></tr>@endforelse</tbody></table></div>{{ $items->links('pagination::bootstrap-5') }}</div>@endsection
