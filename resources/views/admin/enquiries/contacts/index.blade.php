@extends('layouts.admin',['title'=>'Contact Enquiries','description'=>'Review and respond to website contact messages.'])
@section('content')<div class="card admin-card mb-4"><div class="card-body"><form class="row g-3 align-items-end"><div class="col-md-6"><label class="form-label">Search</label><input class="form-control" name="search" value="{{ request('search') }}"></div><div class="col-md-2"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All</option>@foreach(['new','contacted','resolved','spam'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Read state</label><select class="form-select" name="read"><option value="">All</option><option value="unread" @selected(request('read')==='unread')>Unread</option><option value="read" @selected(request('read')==='read')>Read</option></select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-admin-primary">Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.contact-enquiries.index') }}">Reset</a></div></form></div></div><div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle mb-0"><thead><tr><th class="ps-4">Sender</th><th>Subject</th><th>Received</th><th>Read</th><th>Status</th><th></th></tr></thead><tbody>@forelse($enquiries as $enquiry)<tr class="{{ $enquiry->is_read?'':'table-light fw-semibold' }}"><td class="ps-4">{{ $enquiry->name }}<div class="small text-muted">{{ $enquiry->email }}</div></td><td>{{ $enquiry->subject?:'No subject' }}</td><td>{{ $enquiry->created_at->format('d M Y H:i') }}</td><td>{{ $enquiry->is_read?'Read':'Unread' }}</td><td><x-admin.status-badge :status="$enquiry->status" /></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.contact-enquiries.show',$enquiry) }}">View</a></td></tr>@empty<tr><td colspan="6"><div class="admin-empty m-3">No contact enquiries found.</div></td></tr>@endforelse</tbody></table></div>@if($enquiries->hasPages())<div class="card-footer bg-white">{{ $enquiries->links('pagination::bootstrap-5') }}</div>@endif</div>@endsection
