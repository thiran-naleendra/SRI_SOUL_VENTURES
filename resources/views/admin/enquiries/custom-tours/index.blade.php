@extends('layouts.admin',['title'=>'Custom Tour Requests','description'=>'Plan and manage tailor-made tour requests.'])
@section('page-actions')<a class="btn btn-outline-success" href="{{ route('admin.custom-tour-requests.export',request()->query()) }}">Export filtered CSV</a>@endsection
@section('content')<div class="card admin-card mb-4"><div class="card-body"><form class="row g-3 align-items-end"><div class="col-md-5"><label class="form-label">Search</label><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Customer, email, phone or country"></div><div class="col-md-3"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Consultant</label><select class="form-select" name="consultant"><option value="">All</option>@foreach($consultants as $user)<option value="{{ $user->id }}" @selected((string)request('consultant')===(string)$user->id)>{{ $user->name }}</option>@endforeach</select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-admin-primary">Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.custom-tour-requests.index') }}">Reset</a></div></form></div></div><div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle mb-0"><thead><tr><th class="ps-4">Customer</th><th>Dates</th><th>Destinations</th><th>Consultant</th><th>Status</th><th></th></tr></thead><tbody>@forelse($requests as $tour)<tr><td class="ps-4"><strong>{{ $tour->customer_name }}</strong><div class="small">{{ $tour->email }}</div></td><td>{{ $tour->arrival_date?->format('d M')??'—' }} – {{ $tour->departure_date?->format('d M Y')??'—' }}</td><td>{{ $tour->destinations->pluck('name')->join(', ')?:'—' }}</td><td>{{ $tour->assignedUser?->name??'Unassigned' }}</td><td><x-admin.status-badge :status="$tour->status" /></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.custom-tour-requests.show',$tour) }}">View</a></td></tr>@empty<tr><td colspan="6"><div class="admin-empty m-3">No custom tour requests found.</div></td></tr>@endforelse</tbody></table></div>@if($requests->hasPages())<div class="card-footer bg-white">{{ $requests->links('pagination::bootstrap-5') }}</div>@endif</div>@endsection
