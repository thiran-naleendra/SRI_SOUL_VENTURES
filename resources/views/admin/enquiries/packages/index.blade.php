@extends('layouts.admin',['title'=>'Package Enquiries','description'=>'Review and manage enquiries submitted for packages.'])
@section('page-actions')<a class="btn btn-outline-success" href="{{ route('admin.package-enquiries.export',request()->query()) }}">Export filtered CSV</a>@endsection
@section('content')<div class="card admin-card mb-4"><div class="card-body"><form class="row g-3 align-items-end"><div class="col-md-5"><label class="form-label">Search</label><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Customer, email, phone or package"></div><div class="col-md-3"><label class="form-label">Package</label><select class="form-select" name="package"><option value="">All packages</option>@foreach($packages as $package)<option value="{{ $package->id }}" @selected((string)request('package')===(string)$package->id)>{{ $package->title }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-admin-primary">Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.package-enquiries.index') }}">Reset</a></div></form></div></div><div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle mb-0"><thead><tr><th class="ps-4">Customer</th><th>Package</th><th>Contact</th><th>Travel dates</th><th>Status</th><th class="pe-4"></th></tr></thead><tbody>@forelse($enquiries as $enquiry)<tr><td class="ps-4"><strong>{{ $enquiry->customer_name }}</strong><div class="small text-muted">{{ $enquiry->country }}</div></td><td>{{ $enquiry->package->title }}</td><td><a href="mailto:{{ $enquiry->email }}">{{ $enquiry->email }}</a><div>{{ $enquiry->phone }}</div></td><td>{{ $enquiry->preferred_start_date?->format('d M Y')??'—' }} – {{ $enquiry->preferred_end_date?->format('d M Y')??'—' }}</td><td><x-admin.status-badge :status="$enquiry->status" /></td><td class="text-end pe-4"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.package-enquiries.show',$enquiry) }}">View</a></td></tr>@empty<tr><td colspan="6"><div class="admin-empty m-3">No package enquiries found.</div></td></tr>@endforelse</tbody></table></div>@if($enquiries->hasPages())<div class="card-footer bg-white">{{ $enquiries->links('pagination::bootstrap-5') }}</div>@endif</div>@endsection
