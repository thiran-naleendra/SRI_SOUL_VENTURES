@php($item = $item ?? null)
<div class="row">
    <div class="col-lg-8"><x-admin.form-input name="name" label="Name" :value="$item?->name" required /><div class="mb-3"><label for="{{ $descriptionField }}" class="form-label">{{ $descriptionField === 'short_description' ? 'Short description' : 'Description' }}</label><textarea id="{{ $descriptionField }}" name="{{ $descriptionField }}" rows="5" class="form-control @error($descriptionField) is-invalid @enderror">{{ old($descriptionField, $item?->{$descriptionField}) }}</textarea>@error($descriptionField)<div class="invalid-feedback">{{ $message }}</div>@enderror</div>@if($hasIcon)<x-admin.form-input name="icon" label="Icon" :value="$item?->icon" help="Enter an icon class or stored icon identifier." />@endif</div>
    <div class="col-lg-4"><x-admin.form-input name="display_order" label="Display order" type="number" :value="$item?->display_order ?? 0" min="0" required /><div class="form-check form-switch mb-4"><input type="hidden" name="is_active" value="0"><input class="form-check-input" type="checkbox" role="switch" id="is_active" name="is_active" value="1" @checked(old('is_active', $item?->is_active ?? true))><label class="form-check-label" for="is_active">Active</label></div><div class="alert alert-light border small">The URL slug is generated automatically from the name and remains unique.</div></div>
</div>
