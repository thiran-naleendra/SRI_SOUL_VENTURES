@if($errors->any())<div class="alert alert-danger" role="alert">Please correct the highlighted fields.</div>@endif
<div class="card admin-card"><div class="card-body p-4"><div class="row"><div class="col-md-6"><x-admin.form-input name="name" label="Name" :value="$managedUser?->name" required /></div><div class="col-md-6"><x-admin.form-input name="email" label="Email address" type="email" :value="$managedUser?->email" required /></div><div class="col-md-6"><x-admin.form-input name="password" label="Password" type="password" :required="! $managedUser" :help="$managedUser ? 'Leave blank to keep the current password.' : 'At least 12 characters with uppercase, lowercase and a number.'" /></div><div class="col-md-6"><x-admin.form-input name="password_confirmation" label="Confirm password" type="password" :required="! $managedUser" /></div><div class="col-12"><fieldset><legend class="h6 mb-3">Roles</legend><div class="row g-3">@php($selectedRoles=old('roles', $managedUser?->roles->pluck('name')->all() ?? []))@foreach($roles as $role)<div class="col-sm-6 col-lg-3"><label class="role-choice"><input class="form-check-input" type="checkbox" name="roles[]" value="{{ $role->name }}" @checked(in_array($role->name, $selectedRoles, true))><span><strong>{{ str($role->name)->replace('_', ' ')->title() }}</strong><small>{{ $role->permissions_count }} permissions</small></span></label></div>@endforeach</div>@error('roles')<div class="text-danger small mt-2">{{ $message }}</div>@enderror</fieldset></div></div></div></div>
