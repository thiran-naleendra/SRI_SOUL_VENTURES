@extends('layouts.admin',['title'=>$title.'s','description'=>'Manage '.$title.' records.'])
@section('page-actions')<a class="btn btn-admin-primary" href="{{ route($routePrefix.'.create') }}">Add {{ strtolower($title) }}</a>@endsection
@section('content')<div class="card admin-card mb-4"><div class="card-body"><form class="row g-3"><div class="col-md-8"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search"></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All</option><option value="active">Active</option><option value="inactive">Inactive</option><option value="trashed">Trashed</option></select></div><div class="col-md-2"><button class="btn btn-admin-primary">Filter</button></div></form></div></div><div class="card admin-card"><div class="table-responsive"><table class="table admin-table mb-0"><thead><tr><th class="ps-4">{{ $title }}</th><th>Order</th><th>Status</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td class="ps-4"><strong>{{ $item->{$fields[0]} }}</strong>@if($imageField&&$item->{$imageField})<div><img src="{{ Storage::disk('public')->url($item->{$imageField}) }}" alt="" height="45" class="rounded"></div>@endif</td><td>{{ $item->display_order }}</td><td>{{ $item->is_active?'Active':'Inactive' }}</td><td class="text-end">@if($item->trashed())<form class="d-inline" method="POST" action="{{ route($routePrefix.'.restore',$item->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success">Restore</button></form>@else<a class="btn btn-sm btn-outline-primary" href="{{ route($routePrefix.'.edit',$item->id) }}">Edit</a><button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#confirmationModal" data-confirm-action="{{ route($routePrefix.'.destroy',$item->id) }}">Delete</button>@endif</td></tr>@empty<tr><td colspan="4"><div class="admin-empty m-3">No records found.</div></td></tr>@endforelse</tbody></table></div>@if($items->hasPages()){{ $items->links('pagination::bootstrap-5') }}@endif</div>@endsection
