@extends('layouts.admin',['title'=>'Edit Package','description'=>"Update {$package->title} and all related content."])
@section('content')<form method="POST" action="{{ route('admin.packages.save',$package->id) }}" enctype="multipart/form-data" data-package-section-url="{{ route('admin.packages.section',$package->id) }}">@csrf @include('admin.packages.partials.form')<div class="mt-4"><a class="btn btn-light" href="{{ route('admin.packages.index') }}">Back to packages</a></div></form>@endsection
