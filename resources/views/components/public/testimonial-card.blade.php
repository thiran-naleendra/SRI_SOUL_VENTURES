@props(['testimonial', 'lazy' => false])<figure class="card rounded-panel h-100"><div class="card-body p-4">@if($testimonial->customer_image)<img class="testimonial-avatar mb-3" src="{{ Storage::disk('public')->url($testimonial->customer_image) }}" alt="{{ $testimonial->customer_name }}" @if($lazy) loading="lazy" @endif>@endif<div class="text-warning mb-3" aria-label="{{ $testimonial->rating }} out of 5 stars">{{ str_repeat('★',$testimonial->rating) }}</div><blockquote class="fs-5 serif">“{{ $testimonial->testimonial }}”</blockquote><figcaption class="mt-3"><strong>{{ $testimonial->customer_name }}</strong><div class="text-secondary">{{ collect([$testimonial->trip_name,$testimonial->country])->filter()->join(' · ') }}</div></figcaption></div></figure>
